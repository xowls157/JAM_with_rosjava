/* Generated By:JavaCC: Do not edit this line. JAMParser.java */
package uos.ai.jam.parser;

import java.io.ByteArrayInputStream;
import java.io.File;
import java.io.FileReader;
import java.io.IOException;
import java.io.Reader;
import java.util.LinkedList;
import java.util.List;
import java.util.StringTokenizer;

import uos.ai.jam.Interpreter;
import uos.ai.jam.exception.AgentRuntimeException;
import uos.ai.jam.exception.ExceptionManager;
import uos.ai.jam.exception.GlobalCatch;
import uos.ai.jam.expression.Expression;
import uos.ai.jam.expression.FunctionCall;
import uos.ai.jam.expression.ObjectCreation;
import uos.ai.jam.expression.ObjectGetField;
import uos.ai.jam.expression.ObjectInvokeMethod;
import uos.ai.jam.expression.Relation;
import uos.ai.jam.expression.SymbolTable;
import uos.ai.jam.expression.Value;
import uos.ai.jam.expression.Variable;
import uos.ai.jam.expression.condition.Condition;
import uos.ai.jam.expression.condition.ExpressionCondition;
import uos.ai.jam.expression.condition.FactCondition;
import uos.ai.jam.expression.condition.RetrieveCondition;
import uos.ai.jam.plan.Plan;
import uos.ai.jam.plan.PlanContext;
import uos.ai.jam.plan.action.AchieveGoalAction;
import uos.ai.jam.plan.action.Action;
import uos.ai.jam.plan.action.AssertAction;
import uos.ai.jam.plan.action.AssignAction;
import uos.ai.jam.plan.action.FactAction;
import uos.ai.jam.plan.action.FailAction;
import uos.ai.jam.plan.action.GoalAction;
import uos.ai.jam.plan.action.LoadPackageAction;
import uos.ai.jam.plan.action.LoadFromURLAction;
import uos.ai.jam.plan.action.MaintainGoalAction;
import uos.ai.jam.plan.action.ObjectAction;
import uos.ai.jam.plan.action.ObjectInvokeAction;
import uos.ai.jam.plan.action.ParseAction;
import uos.ai.jam.plan.action.PerformGoalAction;
import uos.ai.jam.plan.action.PostAction;
import uos.ai.jam.plan.action.QueryGoalAction;
import uos.ai.jam.plan.action.RetractAction;
import uos.ai.jam.plan.action.RetrieveAction;
import uos.ai.jam.plan.action.SimpleAction;
import uos.ai.jam.plan.action.TestAction;
import uos.ai.jam.plan.action.ThrowAction;
import uos.ai.jam.plan.action.UnpostAction;
import uos.ai.jam.plan.action.UpdateAction;
import uos.ai.jam.plan.constructor.PlanAtomicConstruct;
import uos.ai.jam.plan.constructor.PlanBranchConstruct;
import uos.ai.jam.plan.constructor.PlanConstruct;
import uos.ai.jam.plan.constructor.PlanDoAllConstruct;
import uos.ai.jam.plan.constructor.PlanDoAnyConstruct;
import uos.ai.jam.plan.constructor.PlanDoConstruct;
import uos.ai.jam.plan.constructor.PlanParallelConstruct;
import uos.ai.jam.plan.constructor.PlanSequenceConstruct;
import uos.ai.jam.plan.constructor.PlanSimpleConstruct;
import uos.ai.jam.plan.constructor.PlanTryCatchConstruct;
import uos.ai.jam.plan.constructor.PlanTryCatchEntry;
import uos.ai.jam.plan.constructor.PlanWaitConstruct;
import uos.ai.jam.plan.constructor.PlanWhenConstruct;
import uos.ai.jam.plan.constructor.PlanWhileConstruct;
import uos.ai.jam.util.ClassLoaderEX;

public class JAMParser implements JAMParserConstants {
        private ClassLoaderEX                           _loader = new ClassLoaderEX();

        /**
	 * Command-line interface to parser to facilitate testing.
	 */
        public static void main(String args[]) {
                JAMParser parser;
                Interpreter interpreter = null;

                System.out.println("JAM Parser Version 0.60 + 0.80i: ");

                if (args.length == 0) {
                        System.out.println("Reading from standard input . . .");
                        parser = new JAMParser(new java.io.DataInputStream(System.in));
                } else if (args.length == 1) {
                        System.out.println("Reading from file " + args[0] + " . . .");

                        try {
                                parser = new JAMParser(new java.io.DataInputStream(new java.io.FileInputStream(args[0])));
                        } catch (java.io.FileNotFoundException e) {
                                System.out.println("\u005cn\u005cnFile " + args[0] + " not found!\u005cn\u005cn");
                                return;
                        }
                } else {
                        System.out.println("Usage is one of:");
                        System.out.println("\u005ct java JAMParser < inputfile");
                        System.out.println("OR");
                        System.out.println("\u005ct java JAMParser inputfile");
                        return;
                }

                try {
                        parser.ParseJamAgentDefinition(interpreter);
                        System.out.println("JAM program parsed successfully.");
                } catch (ParseException e) {
                        System.out.println("Encountered parsing exception " + e + " during parse.");
                        e.printStackTrace();
                } catch (Exception e) {
                        System.out.println("Encountered other exception " + e + " during parse.");
                        e.printStackTrace();
                }
        }

        /**
	 * Primary JAM interface to parser
	 */
        public static Interpreter buildInterpreter(String buf) {
                JAMParser parser;
                System.out.println("JAM Parser Version 0.60 + 0.80i: ");
                parser = new JAMParser(new ByteArrayInputStream(buf.getBytes()));

                try {
                        Interpreter interpreter =       null;
                        interpreter = (Interpreter) parser.ParseJamAgentDefinition(interpreter);
                        System.out.println("JAM definition parse successful.");
                        return interpreter;
                } catch (ParseException e) {
                        System.out.println("Encountered parse exception " + e + " during parse.");
                        e.printStackTrace();
                        return null;
                } catch (Exception e) {
                        System.out.println("Encountered other exception " + e +  " during parse.");
                        e.printStackTrace();
                        return null;
                }
        }

        /**
	 * Secondary JAM interface used to internalize agent constructs
	 * from strings during execution.
	 */
        public static Interpreter parseString(Interpreter interpreter, String buf) {
                JAMParser parser;
                System.out.println("JAM Parser Version 0.50+0.87i: ");
                parser = new JAMParser(new ByteArrayInputStream(buf.getBytes()));

                try {
                        interpreter = (Interpreter) parser.ParseJamAgentDefinition(interpreter);
                        System.out.println("JAM string parse successful.");
                        return interpreter;
                } catch (ParseException e) {
                        System.out.println("Encountered parse exception " + e + " during parse.");
                        e.printStackTrace();
                        return null;
                } catch (Exception e) {
                        System.out.println("Encountered other exception " + e + " during parse.");
                        e.printStackTrace();
                        return null;
                }
        }

        public static Interpreter parseFile(Interpreter interpreter, File file) {
                if (file == null) return null;
                String filename = null;
                Reader reader = null;
                try {
                  filename = file.getCanonicalPath();
                  reader = new FileReader(file);
                } catch (IOException e) {
                  e.printStackTrace();
                  return null;
                }

                JAMParser parser;
                System.out.println("JAM Parser Version 0.50+0.87i: " + filename);
                parser = new JAMParser(reader);

                try {
                        interpreter = (Interpreter) parser.ParseJamAgentDefinition(interpreter);
                        System.out.println("JAM string parse successful.");
                        return interpreter;
                } catch (ParseException e) {
                        System.out.println("Encountered parse exception " + e + " during parse.");
                        e.printStackTrace();
                        return null;
                } catch (Exception e) {
                        System.out.println("Encountered other exception " + e + " during parse.");
                        e.printStackTrace();
                        return null;
                }
        }

//
//
//
  final public Object ParseJamAgentDefinition(Interpreter interpreter) throws ParseException {
        if (interpreter == null) interpreter = new Interpreter();
    label_1:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case IMPORT_DECL:
        ;
        break;
      default:
        jj_la1[0] = jj_gen;
        break label_1;
      }
      import_statement();
    }
    agent_components(interpreter);
                {if (true) return interpreter;}
    throw new Error("Missing return statement in function");
  }

  final public void import_statement() throws ParseException {
        Token t = null;
        boolean isPackage = false;
    jj_consume_token(IMPORT_DECL);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case CLASS_IDENTIFIER:
      t = jj_consume_token(CLASS_IDENTIFIER);
      break;
    case IDENTIFIER:
      t = jj_consume_token(IDENTIFIER);
      break;
    default:
      jj_la1[1] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case 109:
      jj_consume_token(109);
      jj_consume_token(STAR);
                                                                                isPackage = true;
      break;
    default:
      jj_la1[2] = jj_gen;
      ;
    }
    jj_consume_token(110);
                if (isPackage) {
                        _loader.importPackage(t.image);
                } else {
                        _loader.importClass(t.image);
                }
  }

/**********************************************************************/
/**                                                                  **/
/**	Agent Components                                                 **/
/**                                                                  **/
/**********************************************************************/
  final public void agent_components(Interpreter interpreter) throws ParseException {
        Plan plan = null;
    label_2:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case GOAL_DECL:
        jj_consume_token(GOAL_DECL);
        label_3:
        while (true) {
          switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
          case ACHIEVE:
          case PERFORM:
          case MAINTAIN:
          case QUERY:
            ;
            break;
          default:
            jj_la1[3] = jj_gen;
            break label_3;
          }
          goal(interpreter);
        }
        break;
      case FACT_DECL:
        jj_consume_token(FACT_DECL);
        label_4:
        while (true) {
          switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
          case FACT:
            ;
            break;
          default:
            jj_la1[4] = jj_gen;
            break label_4;
          }
          fact(interpreter);
        }
        break;
      case OBSERVER_DECL:
        jj_consume_token(OBSERVER_DECL);
        label_5:
        while (true) {
          switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
          case LBRACE:
            ;
            break;
          default:
            jj_la1[5] = jj_gen;
            break label_5;
          }
                                Plan currentPlan = new Plan();
                                PlanSequenceConstruct body_elements = null;
          jj_consume_token(LBRACE);
          body_elements = plan_construct_sequence(currentPlan.getSymbolTable(), interpreter);
          jj_consume_token(RBRACE);
                                currentPlan.setBody(body_elements);
                                interpreter.setObserver(currentPlan);
        }
        break;
      case CATCH:
        jj_consume_token(CATCH);
        globalCatch(interpreter);
        break;
      case PLAN_START:
        plan = plan(interpreter);
                                           interpreter.getPlanLibrary().add(plan);
        break;
      default:
        jj_la1[6] = jj_gen;
        jj_consume_token(-1);
        throw new ParseException();
      }
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case GOAL_DECL:
      case FACT_DECL:
      case OBSERVER_DECL:
      case PLAN_START:
      case CATCH:
        ;
        break;
      default:
        jj_la1[7] = jj_gen;
        break label_2;
      }
    }
  }

/**********************************************************************/
/**                                                                  **/
/**	GOAL                                                             **/
/**                                                                  **/
/**********************************************************************/
  final public void goal(Interpreter interpreter) throws ParseException {
        GoalAction goal = null;
    goal = goal_action(null, interpreter);
    jj_consume_token(110);
                try {
                        interpreter.getIntentionStructure().addUnique(goal, null, null);
                } catch (AgentRuntimeException e) {}
  }

/**********************************************************************/
/**                                                                  **/
/**	FACT                                                             **/
/**                                                                  **/
/**********************************************************************/
  final public void fact(Interpreter interpreter) throws ParseException {
        Relation relation = null;
    jj_consume_token(FACT);
    relation = relation(null, interpreter);
    jj_consume_token(110);
                interpreter.getWorldModel().assertFact(relation, null);
  }

/**********************************************************************/
/**                                                                  **/
/**	Global Catch                                                     **/
/**                                                                  **/
/**********************************************************************/
  final public void globalCatch(Interpreter interpreter) throws ParseException {
        Token en = null;;
        Variable variable = null;
        SymbolTable symbolTable = new SymbolTable();
        PlanConstruct catchConstruct = null;
        Plan currentPlan = new Plan();
    jj_consume_token(LPAREN);
    variable = variable(symbolTable);
    jj_consume_token(111);
    en = jj_consume_token(CLASS_IDENTIFIER);
    jj_consume_token(RPAREN);
    catchConstruct = plan_construct_sequence_block(symbolTable, interpreter);
                ExceptionManager em = interpreter.getExceptionManager();
                em.addGlobalCatch(new GlobalCatch(new String(en.image), variable, catchConstruct));
  }

/**********************************************************************/
/**                                                                  **/
/**	PLAN                                                             **/
/**                                                                  **/
/**********************************************************************/
  final public Plan plan(Interpreter interpreter) throws ParseException {
        Plan plan = new Plan();
        Relation relation = null;
        GoalAction goalAction = null;
    jj_consume_token(PLAN_START);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case PLAN_CONCLUDESPEC:
      jj_consume_token(PLAN_CONCLUDESPEC);
      relation = relation(plan.getSymbolTable(), interpreter);
      break;
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
      goalAction = goal_action(plan.getSymbolTable(), interpreter);
      break;
    default:
      jj_la1[8] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    jj_consume_token(LBRACE);
    plan_components(plan, interpreter);
    jj_consume_token(RBRACE);
                plan.setGoalSpecification(goalAction);
                plan.setConcludeSpecification(relation);
                {if (true) return plan;}
    throw new Error("Missing return statement in function");
  }

/*	
void plan(Interpreter interpreter) :
{}
{ 
	{
		Plan currentPlan = new Plan();
	}
	"{" plan_components(currentPlan, interpreter) "}"
	{
		interpreter.getPlanLibrary().add(currentPlan);
	}
} 
*/
  final public void plan_components(Plan currentPlan, Interpreter interpreter) throws ParseException {
    plan_component(currentPlan, interpreter);
    label_6:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case PLAN_NAME:
      case PLAN_DOC:
      case PLAN_CONTEXT:
      case PLAN_PRECONDITION:
      case PLAN_UTILITY:
      case PLAN_EFFECTS:
      case PLAN_FAILURE:
      case PLAN_ATTRIBUTES:
      case PLAN_BODY:
        ;
        break;
      default:
        jj_la1[9] = jj_gen;
        break label_6;
      }
      plan_component(currentPlan, interpreter);
    }
  }

  final public void plan_component(Plan currentPlan, Interpreter interpreter) throws ParseException {
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case PLAN_NAME:
      plan_name(currentPlan);
      break;
    case PLAN_DOC:
      plan_doc(currentPlan);
      break;
    case PLAN_CONTEXT:
      plan_context(currentPlan, interpreter);
      break;
    case PLAN_PRECONDITION:
      plan_precondition(currentPlan, interpreter);
      break;
    case PLAN_UTILITY:
      plan_utility(currentPlan, interpreter);
      break;
    case PLAN_EFFECTS:
      plan_effects(currentPlan, interpreter);
      break;
    case PLAN_FAILURE:
      plan_failure(currentPlan, interpreter);
      break;
    case PLAN_ATTRIBUTES:
      plan_attributes(currentPlan);
      break;
    case PLAN_BODY:
      plan_body(currentPlan, interpreter);
      break;
    default:
      jj_la1[10] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
  }

/**********************************************************************/
/** Name                                                             **/
/** Doc                                                              **/
/** Attributes                                                       **/
/**********************************************************************/
  final public void plan_name(Plan currentPlan) throws ParseException {
        Token t_s = null;
    jj_consume_token(PLAN_NAME);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
      t_s = jj_consume_token(STRING);
      break;
    default:
      jj_la1[11] = jj_gen;
      ;
    }
                        if (t_s != null)
        currentPlan.setName(t_s.image);
  }

  final public void plan_doc(Plan currentPlan) throws ParseException {
        Token t_s = null;
    jj_consume_token(PLAN_DOC);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
      t_s = jj_consume_token(STRING);
      break;
    default:
      jj_la1[12] = jj_gen;
      ;
    }
                        if (t_s != null)
        currentPlan.setDocumentation(t_s.image);
  }

  final public void plan_attributes(Plan currentPlan) throws ParseException {
        Token t_s = null;
    jj_consume_token(PLAN_ATTRIBUTES);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
      t_s = jj_consume_token(STRING);
      break;
    default:
      jj_la1[13] = jj_gen;
      ;
    }
                        if (t_s != null)
        currentPlan.setAttributes(t_s.image);
  }

/**********************/
/* Goal Specification */
/**********************/
  final public void plan_goalspec(Plan currentPlan, Interpreter interpreter) throws ParseException {
        Relation rel = null;
        Action ga = null;
    jj_consume_token(PLAN_GOALSPEC);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
      ga = goal_action(currentPlan.getSymbolTable(), interpreter);
      jj_consume_token(110);
      break;
    default:
      jj_la1[14] = jj_gen;
      ;
    }
                 if (ga != null) {
                         currentPlan.setGoalSpecification(ga);
                         currentPlan.setConcludeSpecification(rel);
                 }
  }

/*******************************/
/* Plan Conclude Specification */
/*******************************/
  final public void plan_concludespec(Plan currentPlan, Interpreter interpreter) throws ParseException {
        Action ga = null;
        Relation rel = null;
    jj_consume_token(PLAN_CONCLUDESPEC);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case IDENTIFIER:
      rel = relation(currentPlan.getSymbolTable(), interpreter);
      jj_consume_token(110);
      break;
    default:
      jj_la1[15] = jj_gen;
      ;
    }
                 if (rel != null) {
                         currentPlan.setConcludeSpecification(rel);
                         currentPlan.setGoalSpecification(ga);
                 }
  }

/***********/
/* Context */
/***********/
  final public void plan_context(Plan currentPlan, Interpreter interpreter) throws ParseException {
        List<Condition> c = null;
    jj_consume_token(PLAN_CONTEXT);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
    case FACT:
    case RETRIEVE:
    case NEW:
    case LPAREN:
    case BANG:
    case INTEGER:
    case FLOAT:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
    case 112:
      c = condition_list(currentPlan.getSymbolTable(), interpreter);
      break;
    default:
      jj_la1[16] = jj_gen;
      ;
    }
                 if (c != null) {
                         PlanContext context = new PlanContext(c);
                         currentPlan.setContext(context);
                 }
  }

/***********/
/* Context */
/***********/
  final public void plan_precondition(Plan currentPlan, Interpreter interpreter) throws ParseException {
        List<Condition> c = null;
    jj_consume_token(PLAN_PRECONDITION);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
    case FACT:
    case RETRIEVE:
    case NEW:
    case LPAREN:
    case BANG:
    case INTEGER:
    case FLOAT:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
    case 112:
      c = condition_list(currentPlan.getSymbolTable(), interpreter);
      break;
    default:
      jj_la1[17] = jj_gen;
      ;
    }
                 if (c != null) {
                         PlanContext precondition = new PlanContext(c);
                         currentPlan.setPrecondition(precondition);
                 }
  }

/************/
/* Utility */
/************/
  final public void plan_utility(Plan currentPlan, Interpreter interpreter) throws ParseException {
        Expression e = null;
    jj_consume_token(PLAN_UTILITY);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
    case NEW:
    case LPAREN:
    case BANG:
    case INTEGER:
    case FLOAT:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
    case 112:
      e = expression(currentPlan.getSymbolTable(), interpreter);
      jj_consume_token(110);
      break;
    default:
      jj_la1[18] = jj_gen;
      ;
    }
                        if (e != null)
        currentPlan.setUtility(e);
  }

/*********************/
/* Effects / Failure	*/
/*********************/
  final public void plan_effects(Plan currentPlan, Interpreter interpreter) throws ParseException {
        PlanSequenceConstruct body_elements = null;
        PlanAtomicConstruct atomic_element = null;
    jj_consume_token(PLAN_EFFECTS);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case PLAN_BODY_AND:
    case PLAN_BODY_OR:
    case PLAN_BODY_PARALLEL:
    case PLAN_BODY_DO_ALL:
    case PLAN_BODY_DO_ANY:
    case PLAN_BODY_DO:
    case PLAN_BODY_WHILE:
    case PLAN_BODY_WHEN:
    case PLAN_BODY_ATOMIC:
    case PLAN_BODY_WAIT:
    case IF:
    case FOR:
    case EXECUTE:
    case LOAD_PACKAGE:
    case LOAD_FROM_URL:
    case PARSE:
    case FACT:
    case RETRIEVE:
    case IS_TRUE:
    case POST:
    case UNPOST:
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
    case ASSERT:
    case FAIL:
    case RETRACT:
    case UPDATE:
    case TRY:
    case THROW:
    case LBRACE:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      body_elements = plan_construct_sequence(currentPlan.getSymbolTable(), interpreter);
      break;
    default:
      jj_la1[19] = jj_gen;
      ;
    }
                        if (body_elements != null)
        {
                atomic_element = new PlanAtomicConstruct(body_elements);
                currentPlan.setEffects(atomic_element);
        }
  }

  final public void plan_failure(Plan currentPlan, Interpreter interpreter) throws ParseException {
        PlanSequenceConstruct body_elements = null;
        PlanAtomicConstruct atomic_element = null;
    jj_consume_token(PLAN_FAILURE);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case PLAN_BODY_AND:
    case PLAN_BODY_OR:
    case PLAN_BODY_PARALLEL:
    case PLAN_BODY_DO_ALL:
    case PLAN_BODY_DO_ANY:
    case PLAN_BODY_DO:
    case PLAN_BODY_WHILE:
    case PLAN_BODY_WHEN:
    case PLAN_BODY_ATOMIC:
    case PLAN_BODY_WAIT:
    case IF:
    case FOR:
    case EXECUTE:
    case LOAD_PACKAGE:
    case LOAD_FROM_URL:
    case PARSE:
    case FACT:
    case RETRIEVE:
    case IS_TRUE:
    case POST:
    case UNPOST:
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
    case ASSERT:
    case FAIL:
    case RETRACT:
    case UPDATE:
    case TRY:
    case THROW:
    case LBRACE:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      body_elements = plan_construct_sequence(currentPlan.getSymbolTable(), interpreter);
      break;
    default:
      jj_la1[20] = jj_gen;
      ;
    }
                        if (body_elements != null)
        {
                atomic_element = new PlanAtomicConstruct(body_elements);
                currentPlan.setFailure(atomic_element);
        }
  }

/***********/
/* Body		 */
/***********/
  final public void plan_body(Plan currentPlan, Interpreter interpreter) throws ParseException {
        PlanSequenceConstruct body_elements = null;
    jj_consume_token(PLAN_BODY);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case PLAN_BODY_AND:
    case PLAN_BODY_OR:
    case PLAN_BODY_PARALLEL:
    case PLAN_BODY_DO_ALL:
    case PLAN_BODY_DO_ANY:
    case PLAN_BODY_DO:
    case PLAN_BODY_WHILE:
    case PLAN_BODY_WHEN:
    case PLAN_BODY_ATOMIC:
    case PLAN_BODY_WAIT:
    case IF:
    case FOR:
    case EXECUTE:
    case LOAD_PACKAGE:
    case LOAD_FROM_URL:
    case PARSE:
    case FACT:
    case RETRIEVE:
    case IS_TRUE:
    case POST:
    case UNPOST:
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
    case ASSERT:
    case FAIL:
    case RETRACT:
    case UPDATE:
    case TRY:
    case THROW:
    case LBRACE:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      body_elements = plan_construct_sequence(currentPlan.getSymbolTable(), interpreter);
      break;
    default:
      jj_la1[21] = jj_gen;
      ;
    }
                if (body_elements != null) currentPlan.setBody(body_elements);
  }

  final public PlanConstruct plan_construct(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanConstruct ct;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case EXECUTE:
    case LOAD_PACKAGE:
    case LOAD_FROM_URL:
    case PARSE:
    case FACT:
    case RETRIEVE:
    case IS_TRUE:
    case POST:
    case UNPOST:
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
    case ASSERT:
    case FAIL:
    case RETRACT:
    case UPDATE:
    case THROW:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      ct = plan_construct_execute_action(symbolTable, interpreter);
      break;
    case LBRACE:
      ct = plan_construct_sequence_block(symbolTable, interpreter);
      break;
    case PLAN_BODY_AND:
      ct = plan_construct_and(symbolTable, interpreter);
      break;
    case PLAN_BODY_OR:
      ct = plan_construct_or(symbolTable, interpreter);
      break;
    case PLAN_BODY_PARALLEL:
      ct = plan_construct_parallel(symbolTable, interpreter);
      break;
    case PLAN_BODY_DO:
      ct = plan_construct_do(symbolTable, interpreter);
      break;
    case PLAN_BODY_DO_ALL:
      ct = plan_construct_do_all(symbolTable, interpreter);
      break;
    case PLAN_BODY_DO_ANY:
      ct = plan_construct_do_any(symbolTable, interpreter);
      break;
    case PLAN_BODY_WAIT:
      ct = plan_construct_wait(symbolTable, interpreter);
      break;
    case IF:
      ct = plan_construct_if_then_else(symbolTable, interpreter);
      break;
    case PLAN_BODY_WHEN:
      ct = plan_construct_when(symbolTable, interpreter);
      break;
    case PLAN_BODY_WHILE:
      ct = plan_construct_while(symbolTable, interpreter);
      break;
    case FOR:
      ct = plan_construct_for(symbolTable, interpreter);
      break;
    case PLAN_BODY_ATOMIC:
      ct = plan_construct_atomic(symbolTable, interpreter);
      break;
    case TRY:
      ct = plan_construct_try_catch(symbolTable, interpreter);
      break;
    default:
      jj_la1[22] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
                {if (true) return ct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_execute_action(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Action a;
    a = action(symbolTable, interpreter);
    jj_consume_token(110);
                {if (true) return new PlanSimpleConstruct(a);}
    throw new Error("Missing return statement in function");
  }

  final public PlanSequenceConstruct plan_construct_sequence_block(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanSequenceConstruct s;
    jj_consume_token(LBRACE);
    s = plan_construct_sequence(symbolTable, interpreter);
    jj_consume_token(RBRACE);
                {if (true) return s;}
    throw new Error("Missing return statement in function");
  }

  final public PlanSequenceConstruct plan_construct_sequence(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanSequenceConstruct s = new PlanSequenceConstruct();
        PlanConstruct ct;
    ct = plan_construct(symbolTable, interpreter);
                                                          s.insertConstruct(ct);
    label_7:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case PLAN_BODY_AND:
      case PLAN_BODY_OR:
      case PLAN_BODY_PARALLEL:
      case PLAN_BODY_DO_ALL:
      case PLAN_BODY_DO_ANY:
      case PLAN_BODY_DO:
      case PLAN_BODY_WHILE:
      case PLAN_BODY_WHEN:
      case PLAN_BODY_ATOMIC:
      case PLAN_BODY_WAIT:
      case IF:
      case FOR:
      case EXECUTE:
      case LOAD_PACKAGE:
      case LOAD_FROM_URL:
      case PARSE:
      case FACT:
      case RETRIEVE:
      case IS_TRUE:
      case POST:
      case UNPOST:
      case ACHIEVE:
      case PERFORM:
      case MAINTAIN:
      case QUERY:
      case ASSERT:
      case FAIL:
      case RETRACT:
      case UPDATE:
      case TRY:
      case THROW:
      case LBRACE:
      case VARIABLE:
      case IDENTIFIER:
      case CLASS_IDENTIFIER:
        ;
        break;
      default:
        jj_la1[23] = jj_gen;
        break label_7;
      }
      ct = plan_construct(symbolTable, interpreter);
                                                                  s.insertConstruct(ct);
    }
                {if (true) return s;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_and(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanBranchConstruct construct;
        PlanSequenceConstruct s;
    jj_consume_token(PLAN_BODY_AND);
                          construct = new PlanBranchConstruct(null, PlanBranchConstruct.PLAN_AND_BRANCH);
    s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                          construct.addBranch(s);
    label_8:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case LBRACE:
        ;
        break;
      default:
        jj_la1[24] = jj_gen;
        break label_8;
      }
      s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                                  construct.addBranch(s);
    }
    jj_consume_token(110);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_or(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanBranchConstruct construct;
        PlanSequenceConstruct s;
    jj_consume_token(PLAN_BODY_OR);
                         construct = new PlanBranchConstruct(null, PlanBranchConstruct.PLAN_OR_BRANCH);
    s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                    construct.addBranch(s);
    label_9:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case LBRACE:
        ;
        break;
      default:
        jj_la1[25] = jj_gen;
        break label_9;
      }
      s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                            construct.addBranch(s);
    }
    jj_consume_token(110);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_parallel(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanParallelConstruct construct;
        PlanSequenceConstruct s;
    jj_consume_token(PLAN_BODY_PARALLEL);
                               construct = new PlanParallelConstruct();
    s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                    construct.insertConstruct(s);
    label_10:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case LBRACE:
        ;
        break;
      default:
        jj_la1[26] = jj_gen;
        break label_10;
      }
      s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                            construct.insertConstruct(s);
    }
    jj_consume_token(110);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_do(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanDoConstruct construct;
        PlanSequenceConstruct s;
        Action a;
    jj_consume_token(PLAN_BODY_DO);
    s = plan_construct_sequence_block(symbolTable, interpreter);
    jj_consume_token(PLAN_BODY_WHILE);
    jj_consume_token(111);
    a = action(symbolTable, interpreter);
    jj_consume_token(110);
                construct = new PlanDoConstruct(a, s);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_do_all(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanDoAllConstruct construct;
        PlanSequenceConstruct s;
    jj_consume_token(PLAN_BODY_DO_ALL);
                             construct = new PlanDoAllConstruct();
    s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                          construct.addBranch(s);
    label_11:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case LBRACE:
        ;
        break;
      default:
        jj_la1[27] = jj_gen;
        break label_11;
      }
      s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                                  construct.addBranch(s);
    }
    jj_consume_token(110);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_do_any(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanDoAnyConstruct construct;
        PlanSequenceConstruct s;
    jj_consume_token(PLAN_BODY_DO_ANY);
                             construct = new PlanDoAnyConstruct();
    s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                          construct.addBranch(s);
    label_12:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case LBRACE:
        ;
        break;
      default:
        jj_la1[28] = jj_gen;
        break label_12;
      }
      s = plan_construct_sequence_block(symbolTable, interpreter);
                                                                                  construct.addBranch(s);
    }
    jj_consume_token(110);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_wait(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanConstruct c = null;
        Action a = null;
        Relation rel = null;
    jj_consume_token(PLAN_BODY_WAIT);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case 111:
      jj_consume_token(111);
      a = action(symbolTable, interpreter);
      jj_consume_token(110);
                                                                          c = new PlanWaitConstruct(a);
      break;
    case IDENTIFIER:
      rel = relation(symbolTable, interpreter);
      jj_consume_token(110);
                                                                  c = new PlanWaitConstruct(rel);
      break;
    default:
      jj_la1[29] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
                {if (true) return c;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_when(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanWhenConstruct construct;
        PlanSequenceConstruct s;
        Action a;
    jj_consume_token(PLAN_BODY_WHEN);
    jj_consume_token(111);
    a = action(symbolTable, interpreter);
    s = plan_construct_sequence_block(symbolTable, interpreter);
    jj_consume_token(110);
                construct = new PlanWhenConstruct(a, s);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_if_then_else(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression e;
        PlanConstruct s;
        PlanConstruct s2 = null;
    jj_consume_token(IF);
    jj_consume_token(LPAREN);
    e = expression(symbolTable, interpreter);
    jj_consume_token(RPAREN);
    s = plan_construct(symbolTable, interpreter);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case ELSE:
      jj_consume_token(ELSE);
      s2 = plan_construct(symbolTable, interpreter);
      break;
    default:
      jj_la1[30] = jj_gen;
      ;
    }
                {if (true) return new PlanWhenConstruct(new TestAction(e), s, s2);}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_while(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanWhileConstruct construct;
        PlanSequenceConstruct s;
        Action a;
    jj_consume_token(PLAN_BODY_WHILE);
    jj_consume_token(111);
    a = action(symbolTable, interpreter);
    s = plan_construct_sequence_block(symbolTable, interpreter);
    jj_consume_token(110);
                construct = new PlanWhileConstruct(a, s);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_for(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Action s = null;
        Action i = null;
        Expression c = null;
        PlanConstruct b = null;
    jj_consume_token(FOR);
    jj_consume_token(LPAREN);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case EXECUTE:
    case LOAD_PACKAGE:
    case LOAD_FROM_URL:
    case PARSE:
    case FACT:
    case RETRIEVE:
    case IS_TRUE:
    case POST:
    case UNPOST:
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
    case ASSERT:
    case FAIL:
    case RETRACT:
    case UPDATE:
    case THROW:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      s = action(symbolTable, interpreter);
      break;
    default:
      jj_la1[31] = jj_gen;
      ;
    }
    jj_consume_token(110);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
    case NEW:
    case LPAREN:
    case BANG:
    case INTEGER:
    case FLOAT:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
    case 112:
      c = expression(symbolTable, interpreter);
      break;
    default:
      jj_la1[32] = jj_gen;
      ;
    }
    jj_consume_token(110);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case EXECUTE:
    case LOAD_PACKAGE:
    case LOAD_FROM_URL:
    case PARSE:
    case FACT:
    case RETRIEVE:
    case IS_TRUE:
    case POST:
    case UNPOST:
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
    case ASSERT:
    case FAIL:
    case RETRACT:
    case UPDATE:
    case THROW:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      i = action(symbolTable, interpreter);
      break;
    default:
      jj_la1[33] = jj_gen;
      ;
    }
    jj_consume_token(RPAREN);
    b = plan_construct(symbolTable, interpreter);
                PlanSequenceConstruct forConstruct = new PlanSequenceConstruct();
                forConstruct.insertConstruct(new PlanSimpleConstruct(s));

                PlanSequenceConstruct bodyConstruct = new PlanSequenceConstruct();
                bodyConstruct.insertConstruct(b);
                if (i != null) {
                        bodyConstruct.insertConstruct(new PlanSimpleConstruct(i));
                }
                if (c == null) {
                        c = Value.TRUE;
                }
                forConstruct.insertConstruct(new PlanWhileConstruct(new TestAction(c), bodyConstruct));
                {if (true) return forConstruct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_atomic(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanAtomicConstruct construct;
        PlanSequenceConstruct s;
    jj_consume_token(PLAN_BODY_ATOMIC);
    s = plan_construct_sequence_block(symbolTable, interpreter);
    jj_consume_token(110);
                construct = new PlanAtomicConstruct(s);
                {if (true) return construct;}
    throw new Error("Missing return statement in function");
  }

  final public PlanConstruct plan_construct_try_catch(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        PlanConstruct tryConstruct = null;
        Variable variable = null;
        String exceptionName = null;
        Token token = null;
        PlanConstruct catchConstruct = null;
        List<PlanTryCatchEntry> list = new LinkedList<PlanTryCatchEntry>();
    jj_consume_token(TRY);
    tryConstruct = plan_construct_sequence_block(symbolTable, interpreter);
    label_13:
    while (true) {
      jj_consume_token(CATCH);
      jj_consume_token(LPAREN);
      variable = variable(symbolTable);
      jj_consume_token(111);
      token = jj_consume_token(CLASS_IDENTIFIER);
      jj_consume_token(RPAREN);
      catchConstruct = plan_construct_sequence_block(symbolTable, interpreter);
                        list.add(new PlanTryCatchEntry(new String(token.image), variable, catchConstruct));
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case CATCH:
        ;
        break;
      default:
        jj_la1[34] = jj_gen;
        break label_13;
      }
    }
    jj_consume_token(110);
                {if (true) return new PlanTryCatchConstruct(tryConstruct, list.toArray(PlanTryCatchEntry.NULL_ARRAY));}
    throw new Error("Missing return statement in function");
  }

/**********************************************************************/
/*											*/
/*	Condition									*/
/*											*/
/**********************************************************************/
  final public List<Condition> condition_list(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        List<Condition> cl = new LinkedList<Condition>();
        Condition c = null;
    c = condition(symbolTable, interpreter);
                        cl.add(c);
    label_14:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case STRING:
      case FACT:
      case RETRIEVE:
      case NEW:
      case LPAREN:
      case BANG:
      case INTEGER:
      case FLOAT:
      case VARIABLE:
      case IDENTIFIER:
      case CLASS_IDENTIFIER:
      case 112:
        ;
        break;
      default:
        jj_la1[35] = jj_gen;
        break label_14;
      }
      c = condition(symbolTable, interpreter);
                cl.add(c);
    }
                        {if (true) return cl;}
    throw new Error("Missing return statement in function");
  }

  final public Condition condition(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression e;
        Relation relation;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
    case NEW:
    case LPAREN:
    case BANG:
    case INTEGER:
    case FLOAT:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
    case 112:
      e = expression(symbolTable, interpreter);
      jj_consume_token(110);
                        {if (true) return new ExpressionCondition(e);}
      break;
    case FACT:
      jj_consume_token(FACT);
      relation = relation(symbolTable, interpreter);
      jj_consume_token(110);
                        {if (true) return new FactCondition(relation, interpreter);}
      break;
    case RETRIEVE:
      jj_consume_token(RETRIEVE);
      relation = relation(symbolTable, interpreter);
      jj_consume_token(110);
                        {if (true) return new RetrieveCondition(relation, interpreter);}
      break;
    default:
      jj_la1[36] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    throw new Error("Missing return statement in function");
  }

/**********************************************************************/
/*											*/
/*	Action										*/
/*											*/
/**********************************************************************/
  final public Action action(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Action a;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      a = action_expression(symbolTable, interpreter);
      break;
    case IS_TRUE:
      a = action_test(symbolTable, interpreter);
      break;
    case EXECUTE:
      a = action_execute(symbolTable, interpreter);
      break;
    case FAIL:
      a = action_fail();
      break;
    case LOAD_PACKAGE:
      a = action_load_package(symbolTable, interpreter);
      break;
    case LOAD_FROM_URL:
      a = action_load_from_url(symbolTable, interpreter);
      break;
    case PARSE:
      a = action_parse(symbolTable, interpreter);
      break;
    case FACT:
      a = action_fact(symbolTable, interpreter);
      break;
    case RETRIEVE:
      a = action_retrieve(symbolTable, interpreter);
      break;
    case ASSERT:
      a = action_assert(symbolTable, interpreter);
      break;
    case RETRACT:
      a = action_retract(symbolTable, interpreter);
      break;
    case UPDATE:
      a = action_update(symbolTable, interpreter);
      break;
    case POST:
      a = action_post(symbolTable, interpreter);
      break;
    case UNPOST:
      a = action_unpost(symbolTable, interpreter);
      break;
    case ACHIEVE:
    case PERFORM:
    case MAINTAIN:
    case QUERY:
      a = goal_action(symbolTable, interpreter);
      break;
    case THROW:
      a = throw_action(symbolTable, interpreter);
      break;
    default:
      jj_la1[37] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
                {if (true) return a;}
    throw new Error("Missing return statement in function");
  }

  final public Action action_expression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression expr1 = null;
        Expression expr2 = null;
        Action a = null;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case VARIABLE:
      expr1 = variable(symbolTable);
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case ASSIGN:
        jj_consume_token(ASSIGN);
        expr2 = expression(symbolTable, interpreter);
        break;
      case 109:
        label_15:
        while (true) {
          expr1 = obj_operation(expr1, symbolTable, interpreter);
          switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
          case 109:
            ;
            break;
          default:
            jj_la1[38] = jj_gen;
            break label_15;
          }
        }
        break;
      default:
        jj_la1[39] = jj_gen;
        jj_consume_token(-1);
        throw new ParseException();
      }
                if (expr2 != null) {
                        {if (true) return new AssignAction(expr1, expr2);}
                } else {
                        {if (true) return new ObjectInvokeAction(expr1);}
                }
      break;
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      expr1 = cls_operation(symbolTable, interpreter);
      label_16:
      while (true) {
        switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
        case 109:
          ;
          break;
        default:
          jj_la1[40] = jj_gen;
          break label_16;
        }
        expr1 = obj_operation(expr1, symbolTable, interpreter);
      }
                {if (true) return new ObjectInvokeAction(expr1);}
      break;
    default:
      jj_la1[41] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    throw new Error("Missing return statement in function");
  }

/*
Action action_assign(SymbolTable symbolTable, Interpreter interpreter) :
{
	Expression v;
	Expression e;
}
{
	v=variable(symbolTable) <ASSIGN> e=expression(symbolTable, interpreter)
	{
		return new AssignAction(v, e);
	}
}
*/
  final public Action action_execute(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Token cls = null;
        Token fn = null;
        Variable v = null;
        List<Expression> el = null;
    jj_consume_token(EXECUTE);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case CLASS_IDENTIFIER:
      cls = jj_consume_token(CLASS_IDENTIFIER);
      break;
    case 112:
      jj_consume_token(112);
      fn = jj_consume_token(IDENTIFIER);
      break;
    default:
      jj_la1[42] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case LBRACKET:
      jj_consume_token(LBRACKET);
      v = variable(symbolTable);
      jj_consume_token(RBRACKET);
      break;
    default:
      jj_la1[43] = jj_gen;
      ;
    }
    el = object_arguments(symbolTable, interpreter);
                if (cls != null || v != null) {

                        // Break down the class identifier into the class path and the function.
                        String fullIdentifier = null;
                        if (cls != null) {
                                fullIdentifier = new String(cls.image);
                        } else {
                                fullIdentifier = new String(fn.image);
                        }

                        //	System.out.println("fullIdentifier = " + fullIdentifier);
                        int lastDotIndex = fullIdentifier.lastIndexOf(".");
                        //	System.out.println("lastDotIndex = " + lastDotIndex);
                        String funcName = fullIdentifier.substring(lastDotIndex+1);
                        //	System.out.println("funcName = " + funcName);

                        String className = null;
                        if (lastDotIndex != -1) {
                                className = fullIdentifier.substring(0, lastDotIndex);
                        } else {
                                className = new String("");
                        }

                        //	System.out.println("className = " + className);
                        //	System.out.println("arg list = " + el + ", #args = " + el.getCount());

                        if (v != null) {
                                {if (true) return new ObjectAction(className, funcName, v, el);}
                        } else {
                                {if (true) return new ObjectAction(className, funcName, el);}
                        }
                } else {
                        {if (true) return new SimpleAction(interpreter, fn.image, el);}
                }
    throw new Error("Missing return statement in function");
  }

  final public Action action_load_package(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        List<Expression> el = null;
    jj_consume_token(LOAD_PACKAGE);
    el = object_arguments(symbolTable, interpreter);
                {if (true) return new LoadPackageAction(el, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_load_from_url(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        List<Expression> el = null;
    jj_consume_token(LOAD_FROM_URL);
    el = object_arguments(symbolTable, interpreter);
                {if (true) return new LoadFromURLAction(el, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_parse(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        List<Expression> el = null;
    jj_consume_token(PARSE);
    el = object_arguments(symbolTable, interpreter);
                {if (true) return new ParseAction(el);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_retrieve(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation r;
    jj_consume_token(RETRIEVE);
    r = relation(symbolTable, interpreter);
                {if (true) return new RetrieveAction(r, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_test(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression e;
    jj_consume_token(IS_TRUE);
    e = expression(symbolTable, interpreter);
                {if (true) return new TestAction(e);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_assert(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation r;
    jj_consume_token(ASSERT);
    r = relation(symbolTable, interpreter);
                {if (true) return new AssertAction(r, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_retract(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation r;
    jj_consume_token(RETRACT);
    r = relation(symbolTable, interpreter);
                {if (true) return new RetractAction(r, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_update(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation oldRel;
        Relation newRel;
    jj_consume_token(UPDATE);
    jj_consume_token(LPAREN);
    oldRel = relation(symbolTable, interpreter);
    jj_consume_token(RPAREN);
    jj_consume_token(LPAREN);
    newRel = relation(symbolTable, interpreter);
    jj_consume_token(RPAREN);
                {if (true) return new UpdateAction(oldRel, newRel, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_fact(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation relation;
    jj_consume_token(FACT);
    relation = relation(symbolTable, interpreter);
                {if (true) return new FactAction(relation, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_fail() throws ParseException {
    jj_consume_token(FAIL);
                {if (true) return new FailAction();}
    throw new Error("Missing return statement in function");
  }

  final public Action action_post(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        GoalAction ga;
    jj_consume_token(POST);
    ga = goal_action(symbolTable, interpreter);
                {if (true) return new PostAction(ga, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public Action action_unpost(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        GoalAction ga;
    jj_consume_token(UNPOST);
    ga = goal_action(symbolTable, interpreter);
                {if (true) return new UnpostAction(ga, interpreter);}
    throw new Error("Missing return statement in function");
  }

//-------------------------------------------------------------------------------
// GOAL_ACTION
//-------------------------------------------------------------------------------
  final public GoalAction goal_action(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Token kw;
        GoalAction gab;
        Expression e_utility = null;
        List<Expression> e_by = null;
        List<Expression> e_not_by = null;
    gab = goal_action_basic(symbolTable, interpreter);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case KEYWORD_UTILITY:
      jj_consume_token(KEYWORD_UTILITY);
      e_utility = expression(symbolTable, interpreter);
      break;
    default:
      jj_la1[44] = jj_gen;
      ;
    }
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case KEYWORD_BY:
      jj_consume_token(KEYWORD_BY);
      e_by = object_arguments(symbolTable, interpreter);
      break;
    default:
      jj_la1[45] = jj_gen;
      ;
    }
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case KEYWORD_NOT_BY:
      jj_consume_token(KEYWORD_NOT_BY);
      e_not_by = object_arguments(symbolTable, interpreter);
      break;
    default:
      jj_la1[46] = jj_gen;
      ;
    }
                if (e_utility != null) {
                        gab.setUtility(e_utility);
                }
                if (e_by != null) {
                        gab.setBy(e_by);
                }
                if (e_not_by != null) {
                        gab.setNotBy(e_not_by);
                }
                {if (true) return gab;}
    throw new Error("Missing return statement in function");
  }

  final public GoalAction goal_action_basic(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        GoalAction a;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case ACHIEVE:
      a = goal_action_basic_achieve(symbolTable, interpreter);
      break;
    case PERFORM:
      a = goal_action_basic_perform(symbolTable, interpreter);
      break;
    case MAINTAIN:
      a = goal_action_basic_maintain(symbolTable, interpreter);
      break;
    case QUERY:
      a = goal_action_basic_query(symbolTable, interpreter);
      break;
    default:
      jj_la1[47] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
                {if (true) return a;}
    throw new Error("Missing return statement in function");
  }

  final public GoalAction goal_action_basic_achieve(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation rel;
    jj_consume_token(ACHIEVE);
    rel = relation(symbolTable, interpreter);
                {if (true) return new AchieveGoalAction(rel.getName(), rel, null, null, null, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public GoalAction goal_action_basic_perform(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation rel;
    jj_consume_token(PERFORM);
    rel = relation(symbolTable, interpreter);
                {if (true) return new PerformGoalAction(rel.getName(), rel, null, null, null, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public GoalAction goal_action_basic_maintain(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation rel;
    jj_consume_token(MAINTAIN);
    rel = relation(symbolTable, interpreter);
                {if (true) return new MaintainGoalAction(rel.getName(), rel, null, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public GoalAction goal_action_basic_query(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Relation rel;
    jj_consume_token(QUERY);
    rel = relation(symbolTable, interpreter);
                {if (true) return new QueryGoalAction(rel.getName(), rel, null, interpreter);}
    throw new Error("Missing return statement in function");
  }

  final public ThrowAction throw_action(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression expr = null;
        Token id = null;
        Expression[] args = Expression.NULL_ARRAY;
    jj_consume_token(THROW);
    jj_consume_token(NEW);
    id = jj_consume_token(CLASS_IDENTIFIER);
    jj_consume_token(LPAREN);
    args = _arguments(symbolTable, interpreter);
    jj_consume_token(RPAREN);
                if (expr != null) {
                        {if (true) return new ThrowAction(expr);}
                } else {
                        {if (true) return new ThrowAction(new String(id.image), args);}
                }
    throw new Error("Missing return statement in function");
  }

  final public Expression[] _arguments(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        List<Expression> list = new LinkedList<Expression>();
        Expression e = null;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
    case NEW:
    case LPAREN:
    case BANG:
    case INTEGER:
    case FLOAT:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
    case 112:
      e = expression(symbolTable, interpreter);
                                                         list.add(e);
      label_17:
      while (true) {
        switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
        case STRING:
        case NEW:
        case LPAREN:
        case BANG:
        case INTEGER:
        case FLOAT:
        case VARIABLE:
        case IDENTIFIER:
        case CLASS_IDENTIFIER:
        case 112:
          ;
          break;
        default:
          jj_la1[48] = jj_gen;
          break label_17;
        }
        e = expression(symbolTable, interpreter);
                                                         list.add(e);
      }
      break;
    default:
      jj_la1[49] = jj_gen;
      ;
    }
         {if (true) return list.toArray(Expression.NULL_ARRAY);}
    throw new Error("Missing return statement in function");
  }

/**********************************************************************/
/*											*/
/*	Relation									*/
/*											*/
/**********************************************************************/
  final public Relation relation(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Token id = null;
        List<Expression> el = null;
    id = jj_consume_token(IDENTIFIER);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case LPAREN:
      el = object_arguments(symbolTable, interpreter);
      break;
    default:
      jj_la1[50] = jj_gen;
      ;
    }
                        {if (true) return interpreter.getWorldModel().newRelation(id.image, el);}
    throw new Error("Missing return statement in function");
  }

/**********************************************************************/
/*																	*/
/*	Expression														*/
/*																	*/
/**********************************************************************/
  final public List<Expression> object_arguments(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        List<Expression> exprList = new LinkedList<Expression>();
        Expression e = null;
    jj_consume_token(LPAREN);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case STRING:
    case NEW:
    case LPAREN:
    case BANG:
    case INTEGER:
    case FLOAT:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
    case 112:
      e = expression(symbolTable, interpreter);
                        exprList.add(e);
      label_18:
      while (true) {
        switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
        case 113:
          ;
          break;
        default:
          jj_la1[51] = jj_gen;
          break label_18;
        }
        jj_consume_token(113);
        e = expression(symbolTable, interpreter);
                                exprList.add(e);
      }
      break;
    default:
      jj_la1[52] = jj_gen;
      ;
    }
    jj_consume_token(RPAREN);
                {if (true) return exprList;}
    throw new Error("Missing return statement in function");
  }

/*
List<Expression> explist(SymbolTable symbolTable, Interpreter interpreter) :
{
	List<Expression> el = new LinkedList<Expression>();
	Expression e = null;
}
{
	[
		e=expression(symbolTable, interpreter) 
		{
			el.add(e);
		}
		( e=expression(symbolTable, interpreter)
			{
				el.add(e);
			}
		)*
	]
	{
		return el;
	}
}
*/
  final public Expression expression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression expr = null;
    expr = conditionalExpression(symbolTable, interpreter);
                {if (true) return expr;}
    throw new Error("Missing return statement in function");
  }

  final public Expression conditionalExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression expr = null;
    expr = conditionalOrExpression(symbolTable, interpreter);
                {if (true) return expr;}
    throw new Error("Missing return statement in function");
  }

  final public Expression conditionalOrExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression lhExpr = null;
        Expression rhExpr = null;
    lhExpr = conditionalAndExpression(symbolTable, interpreter);
    label_19:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case SC_OR:
        ;
        break;
      default:
        jj_la1[53] = jj_gen;
        break label_19;
      }
      jj_consume_token(SC_OR);
      rhExpr = conditionalAndExpression(symbolTable, interpreter);
                        List<Expression> exprList = new LinkedList<Expression>();
                        exprList.add(lhExpr);
                        exprList.add(rhExpr);
                        lhExpr = new FunctionCall(interpreter, "||", exprList);
    }
                {if (true) return lhExpr;}
    throw new Error("Missing return statement in function");
  }

  final public Expression conditionalAndExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression lhExpr = null;
        Expression rhExpr = null;
    lhExpr = equalityExpression(symbolTable, interpreter);
    label_20:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case SC_AND:
        ;
        break;
      default:
        jj_la1[54] = jj_gen;
        break label_20;
      }
      jj_consume_token(SC_AND);
      rhExpr = equalityExpression(symbolTable, interpreter);
                        List<Expression> exprList = new LinkedList<Expression>();
                        exprList.add(lhExpr);
                        exprList.add(rhExpr);
                        lhExpr = new FunctionCall(interpreter, "&&", exprList);
    }
                {if (true) return lhExpr;}
    throw new Error("Missing return statement in function");
  }

  final public Expression equalityExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression lhExpr = null;
        Expression rhExpr = null;
        Token t = null;
    lhExpr = relationalExpression(symbolTable, interpreter);
    label_21:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case EQ:
      case NE:
        ;
        break;
      default:
        jj_la1[55] = jj_gen;
        break label_21;
      }
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case EQ:
        t = jj_consume_token(EQ);
        break;
      case NE:
        t = jj_consume_token(NE);
        break;
      default:
        jj_la1[56] = jj_gen;
        jj_consume_token(-1);
        throw new ParseException();
      }
      rhExpr = relationalExpression(symbolTable, interpreter);
                        List<Expression> exprList = new LinkedList<Expression>();
                        exprList.add(lhExpr);
                        exprList.add(rhExpr);
                        lhExpr = new FunctionCall(interpreter, t.image, exprList);
    }
                {if (true) return lhExpr;}
    throw new Error("Missing return statement in function");
  }

  final public Expression relationalExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression lhExpr = null;
        Expression rhExpr = null;
        Token t = null;
    lhExpr = additiveExpression(symbolTable, interpreter);
    label_22:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case GT:
      case LT:
      case LE:
      case GE:
        ;
        break;
      default:
        jj_la1[57] = jj_gen;
        break label_22;
      }
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case LT:
        t = jj_consume_token(LT);
        break;
      case GT:
        t = jj_consume_token(GT);
        break;
      case LE:
        t = jj_consume_token(LE);
        break;
      case GE:
        t = jj_consume_token(GE);
        break;
      default:
        jj_la1[58] = jj_gen;
        jj_consume_token(-1);
        throw new ParseException();
      }
      rhExpr = additiveExpression(symbolTable, interpreter);
                        List<Expression> exprList = new LinkedList<Expression>();
                        exprList.add(lhExpr);
                        exprList.add(rhExpr);
                        lhExpr = new FunctionCall(interpreter, t.image, exprList);
    }
                {if (true) return lhExpr;}
    throw new Error("Missing return statement in function");
  }

  final public Expression additiveExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression lhExpr = null;
        Expression rhExpr = null;
        Token t = null;
    lhExpr = multiplicativeExpression(symbolTable, interpreter);
    label_23:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case PLUS:
      case MINUS:
        ;
        break;
      default:
        jj_la1[59] = jj_gen;
        break label_23;
      }
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case PLUS:
        t = jj_consume_token(PLUS);
        break;
      case MINUS:
        t = jj_consume_token(MINUS);
        break;
      default:
        jj_la1[60] = jj_gen;
        jj_consume_token(-1);
        throw new ParseException();
      }
      rhExpr = multiplicativeExpression(symbolTable, interpreter);
                        List<Expression> exprList = new LinkedList<Expression>();
                        exprList.add(lhExpr);
                        exprList.add(rhExpr);
                        lhExpr = new FunctionCall(interpreter, t.image, exprList);
    }
                {if (true) return lhExpr;}
    throw new Error("Missing return statement in function");
  }

  final public Expression multiplicativeExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression lhExpr = null;
        Expression rhExpr = null;
        Token t = null;
    lhExpr = unaryExpression(symbolTable, interpreter);
    label_24:
    while (true) {
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case STAR:
      case SLASH:
        ;
        break;
      default:
        jj_la1[61] = jj_gen;
        break label_24;
      }
      switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
      case STAR:
        t = jj_consume_token(STAR);
        break;
      case SLASH:
        t = jj_consume_token(SLASH);
        break;
      default:
        jj_la1[62] = jj_gen;
        jj_consume_token(-1);
        throw new ParseException();
      }
      rhExpr = unaryExpression(symbolTable, interpreter);
                        List<Expression> exprList = new LinkedList<Expression>();
                        exprList.add(lhExpr);
                        exprList.add(rhExpr);
                        lhExpr = new FunctionCall(interpreter, t.image, exprList);
    }
                {if (true) return lhExpr;}
    throw new Error("Missing return statement in function");
  }

  final public Expression unaryExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression expr = null;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case BANG:
      jj_consume_token(BANG);
      expr = unaryExpression(symbolTable, interpreter);
                List<Expression> exprList = new LinkedList<Expression>();
                exprList.add(expr);
                {if (true) return new FunctionCall(interpreter, "!", exprList);}
      break;
    case STRING:
    case NEW:
    case LPAREN:
    case INTEGER:
    case FLOAT:
    case VARIABLE:
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
    case 112:
      expr = primaryExpression(symbolTable, interpreter);
                {if (true) return expr;}
      break;
    default:
      jj_la1[63] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    throw new Error("Missing return statement in function");
  }

  final public Expression primaryExpression(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Expression expr = null;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case LPAREN:
      jj_consume_token(LPAREN);
      expr = expression(symbolTable, interpreter);
      jj_consume_token(RPAREN);
      break;
    case 112:
      expr = funcall(symbolTable, interpreter);
      break;
    case VARIABLE:
      expr = variable(symbolTable);
      label_25:
      while (true) {
        switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
        case 109:
          ;
          break;
        default:
          jj_la1[64] = jj_gen;
          break label_25;
        }
        expr = obj_operation(expr, symbolTable, interpreter);
      }
      break;
    case STRING:
    case INTEGER:
    case FLOAT:
      expr = value();
      break;
    case NEW:
      expr = obj_creation(symbolTable, interpreter);
      label_26:
      while (true) {
        switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
        case 109:
          ;
          break;
        default:
          jj_la1[65] = jj_gen;
          break label_26;
        }
        expr = obj_operation(expr, symbolTable, interpreter);
      }
      break;
    case IDENTIFIER:
    case CLASS_IDENTIFIER:
      expr = cls_operation(symbolTable, interpreter);
      label_27:
      while (true) {
        switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
        case 109:
          ;
          break;
        default:
          jj_la1[66] = jj_gen;
          break label_27;
        }
        expr = obj_operation(expr, symbolTable, interpreter);
      }
      break;
    default:
      jj_la1[67] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
                {if (true) return expr;}
    throw new Error("Missing return statement in function");
  }

  final public Value value() throws ParseException {
        Token v;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case INTEGER:
      v = jj_consume_token(INTEGER);
                                  {if (true) return new Value(Integer.valueOf(v.image).intValue());}
      break;
    case FLOAT:
      v = jj_consume_token(FLOAT);
                                  {if (true) return new Value(Double.valueOf(v.image).doubleValue());}
      break;
    case STRING:
      v = jj_consume_token(STRING);
                                  {if (true) return new Value(v.image);}
      break;
    default:
      jj_la1[68] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    throw new Error("Missing return statement in function");
  }

  final public Variable variable(SymbolTable symbolTable) throws ParseException {
        Token v;
    v = jj_consume_token(VARIABLE);
                {if (true) return new Variable(symbolTable.getSymbol(v.image));}
    throw new Error("Missing return statement in function");
  }

  final public FunctionCall funcall(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Token fn;
        List<Expression> el;
    jj_consume_token(112);
    fn = jj_consume_token(IDENTIFIER);
    el = object_arguments(symbolTable, interpreter);
                {if (true) return new FunctionCall(interpreter, fn.image, el);}
    throw new Error("Missing return statement in function");
  }

  final public ObjectCreation obj_creation(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Token t;
        List<Expression> exprList;
    jj_consume_token(NEW);
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case CLASS_IDENTIFIER:
      t = jj_consume_token(CLASS_IDENTIFIER);
      break;
    case IDENTIFIER:
      t = jj_consume_token(IDENTIFIER);
      break;
    default:
      jj_la1[69] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    exprList = object_arguments(symbolTable, interpreter);
                Class<?> clazz = null;
                try {
                        clazz = _loader.loadClass(t.image);
                } catch(ClassNotFoundException ex) {
                        {if (true) throw new ParseException(ex.getLocalizedMessage());}
                }
                {if (true) return new ObjectCreation(clazz, exprList);}
    throw new Error("Missing return statement in function");
  }

  final public Expression cls_operation(SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Token t = null;
        List<Expression> expressionList = null;
    switch ((jj_ntk==-1)?jj_ntk():jj_ntk) {
    case CLASS_IDENTIFIER:
      t = jj_consume_token(CLASS_IDENTIFIER);
      break;
    case IDENTIFIER:
      t = jj_consume_token(IDENTIFIER);
      break;
    default:
      jj_la1[70] = jj_gen;
      jj_consume_token(-1);
      throw new ParseException();
    }
    if (jj_2_1(2)) {
      expressionList = object_arguments(symbolTable, interpreter);
    } else {
      ;
    }
                try {
                        StringTokenizer tokenizer = new StringTokenizer(t.image, ".");
                        Class<?> clazz = _loader.loadClass(tokenizer);
                        Expression expression = new Value(clazz);
                        if (!tokenizer.hasMoreTokens()) {
                                {if (true) throw new ParseException("require: field/method name");}
                        }
                        while(tokenizer.hasMoreTokens()) {
                                String token = tokenizer.nextToken();
                                if (tokenizer.hasMoreTokens()) {
                                        expression = new ObjectGetField(expression, token);
                                } else {
                                        /*
					 * MixedClassOperation은 xx.xx.xx.xx() 또는 xx.xx.xx.xx 가 올 수 있다.
					 * 마지막 부분에서 call method 인지 get field 인지 검사해야 한다.
					 */
                                        if (expressionList != null) {
                                                expression = new ObjectInvokeMethod(expression, token, expressionList);
                                        } else {
                                                expression = new ObjectGetField(expression, token);
                                        }
                                }
                        }
                        {if (true) return expression;}
                } catch(ClassNotFoundException e) {
                        {if (true) throw new ParseException(e.getLocalizedMessage());}
                }
    throw new Error("Missing return statement in function");
  }

  final public Expression obj_operation(Expression expression, SymbolTable symbolTable, Interpreter interpreter) throws ParseException {
        Token t = null;
        List<Expression> expressionList = null;
    label_28:
    while (true) {
      jj_consume_token(109);
      t = jj_consume_token(IDENTIFIER);
      if (jj_2_2(2)) {
        expressionList = object_arguments(symbolTable, interpreter);
      } else {
        ;
      }
                        if (expressionList == null) {
                                {if (true) return new ObjectGetField(expression, t.image);}
                        } else {
                                {if (true) return new ObjectInvokeMethod(expression, t.image, expressionList);}
                        }
                        expressionList = null;
      if (jj_2_3(2)) {
        ;
      } else {
        break label_28;
      }
    }
                {if (true) return expression;}
    throw new Error("Missing return statement in function");
  }

  private boolean jj_2_1(int xla) {
    jj_la = xla; jj_lastpos = jj_scanpos = token;
    try { return !jj_3_1(); }
    catch(LookaheadSuccess ls) { return true; }
    finally { jj_save(0, xla); }
  }

  private boolean jj_2_2(int xla) {
    jj_la = xla; jj_lastpos = jj_scanpos = token;
    try { return !jj_3_2(); }
    catch(LookaheadSuccess ls) { return true; }
    finally { jj_save(1, xla); }
  }

  private boolean jj_2_3(int xla) {
    jj_la = xla; jj_lastpos = jj_scanpos = token;
    try { return !jj_3_3(); }
    catch(LookaheadSuccess ls) { return true; }
    finally { jj_save(2, xla); }
  }

  private boolean jj_3R_52() {
    if (jj_scan_token(NEW)) return true;
    return false;
  }

  private boolean jj_3R_42() {
    Token xsp;
    xsp = jj_scanpos;
    if (jj_3R_43()) {
    jj_scanpos = xsp;
    if (jj_3R_44()) {
    jj_scanpos = xsp;
    if (jj_3R_45()) {
    jj_scanpos = xsp;
    if (jj_3R_46()) {
    jj_scanpos = xsp;
    if (jj_3R_47()) {
    jj_scanpos = xsp;
    if (jj_3R_48()) return true;
    }
    }
    }
    }
    }
    return false;
  }

  private boolean jj_3R_33() {
    if (jj_3R_34()) return true;
    return false;
  }

  private boolean jj_3R_49() {
    if (jj_scan_token(112)) return true;
    return false;
  }

  private boolean jj_3R_41() {
    if (jj_3R_42()) return true;
    return false;
  }

  private boolean jj_3R_35() {
    if (jj_3R_36()) return true;
    return false;
  }

  private boolean jj_3R_37() {
    if (jj_3R_38()) return true;
    return false;
  }

  private boolean jj_3R_32() {
    if (jj_3R_33()) return true;
    return false;
  }

  private boolean jj_3R_39() {
    Token xsp;
    xsp = jj_scanpos;
    if (jj_3R_40()) {
    jj_scanpos = xsp;
    if (jj_3R_41()) return true;
    }
    return false;
  }

  private boolean jj_3R_40() {
    if (jj_scan_token(BANG)) return true;
    return false;
  }

  private boolean jj_3R_50() {
    if (jj_scan_token(VARIABLE)) return true;
    return false;
  }

  private boolean jj_3R_30() {
    if (jj_3R_31()) return true;
    return false;
  }

  private boolean jj_3_1() {
    if (jj_3R_29()) return true;
    return false;
  }

  private boolean jj_3R_53() {
    Token xsp;
    xsp = jj_scanpos;
    if (jj_scan_token(108)) {
    jj_scanpos = xsp;
    if (jj_scan_token(107)) return true;
    }
    return false;
  }

  private boolean jj_3_3() {
    if (jj_scan_token(109)) return true;
    if (jj_scan_token(IDENTIFIER)) return true;
    return false;
  }

  private boolean jj_3R_56() {
    if (jj_scan_token(STRING)) return true;
    return false;
  }

  private boolean jj_3R_29() {
    if (jj_scan_token(LPAREN)) return true;
    Token xsp;
    xsp = jj_scanpos;
    if (jj_3R_30()) jj_scanpos = xsp;
    if (jj_scan_token(RPAREN)) return true;
    return false;
  }

  private boolean jj_3R_55() {
    if (jj_scan_token(FLOAT)) return true;
    return false;
  }

  private boolean jj_3R_31() {
    if (jj_3R_32()) return true;
    return false;
  }

  private boolean jj_3R_54() {
    if (jj_scan_token(INTEGER)) return true;
    return false;
  }

  private boolean jj_3R_51() {
    Token xsp;
    xsp = jj_scanpos;
    if (jj_3R_54()) {
    jj_scanpos = xsp;
    if (jj_3R_55()) {
    jj_scanpos = xsp;
    if (jj_3R_56()) return true;
    }
    }
    return false;
  }

  private boolean jj_3R_48() {
    if (jj_3R_53()) return true;
    return false;
  }

  private boolean jj_3R_34() {
    if (jj_3R_35()) return true;
    return false;
  }

  private boolean jj_3R_47() {
    if (jj_3R_52()) return true;
    return false;
  }

  private boolean jj_3R_46() {
    if (jj_3R_51()) return true;
    return false;
  }

  private boolean jj_3R_45() {
    if (jj_3R_50()) return true;
    return false;
  }

  private boolean jj_3_2() {
    if (jj_3R_29()) return true;
    return false;
  }

  private boolean jj_3R_36() {
    if (jj_3R_37()) return true;
    return false;
  }

  private boolean jj_3R_44() {
    if (jj_3R_49()) return true;
    return false;
  }

  private boolean jj_3R_38() {
    if (jj_3R_39()) return true;
    return false;
  }

  private boolean jj_3R_43() {
    if (jj_scan_token(LPAREN)) return true;
    return false;
  }

  /** Generated Token Manager. */
  public JAMParserTokenManager token_source;
  SimpleCharStream jj_input_stream;
  /** Current token. */
  public Token token;
  /** Next token. */
  public Token jj_nt;
  private int jj_ntk;
  private Token jj_scanpos, jj_lastpos;
  private int jj_la;
  private int jj_gen;
  final private int[] jj_la1 = new int[71];
  static private int[] jj_la1_0;
  static private int[] jj_la1_1;
  static private int[] jj_la1_2;
  static private int[] jj_la1_3;
  static {
      jj_la1_init_0();
      jj_la1_init_1();
      jj_la1_init_2();
      jj_la1_init_3();
   }
   private static void jj_la1_init_0() {
      jj_la1_0 = new int[] {0x10000000,0x0,0x0,0x0,0x0,0x0,0xe0000000,0xe0000000,0x0,0x0,0x0,0x400,0x400,0x400,0x0,0x0,0x400,0x400,0x400,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x400,0x0,0x0,0x400,0x400,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x400,0x400,0x0,0x0,0x400,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x400,0x0,0x0,0x0,0x400,0x400,0x0,0x0,};
   }
   private static void jj_la1_init_1() {
      jj_la1_1 = new int[] {0x0,0x0,0x0,0x0,0x0,0x0,0x1,0x1,0x10,0xfe6,0xfe6,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0xf17ff000,0xf17ff000,0xf17ff000,0xf17ff000,0xf17ff000,0x0,0x0,0x0,0x0,0x0,0x0,0x800000,0xf0000000,0x0,0xf0000000,0x0,0x0,0x0,0xf0000000,0x0,0x0,0x0,0x0,0x0,0x0,0x2000000,0x4000000,0x8000000,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,};
   }
   private static void jj_la1_init_2() {
      jj_la1_2 = new int[] {0x0,0x0,0x0,0x1e0,0x1,0x80000,0x4000,0x4000,0x1e0,0x0,0x0,0x0,0x0,0x0,0x1e0,0x0,0x4030003,0x4030003,0x4030000,0x8bfff,0x8bfff,0x8bfff,0x8bfff,0x8bfff,0x80000,0x80000,0x80000,0x80000,0x80000,0x0,0x0,0x9fff,0x4030000,0x9fff,0x4000,0x4030003,0x4030003,0x9fff,0x0,0x800000,0x0,0x0,0x0,0x200000,0x0,0x0,0x0,0x1e0,0x4030000,0x4030000,0x20000,0x0,0x4030000,0x80000000,0x0,0x48000000,0x48000000,0x33000000,0x33000000,0x0,0x0,0x0,0x0,0x4030000,0x0,0x0,0x0,0x30000,0x0,0x0,0x0,};
   }
   private static void jj_la1_init_3() {
      jj_la1_3 = new int[] {0x0,0x1800,0x2000,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x800,0x11d80,0x11d80,0x11d80,0x1c00,0x1c00,0x1c00,0x1c00,0x1c00,0x0,0x0,0x0,0x0,0x0,0x8800,0x0,0x1c00,0x11d80,0x1c00,0x0,0x11d80,0x11d80,0x1c00,0x2000,0x2000,0x2000,0x1c00,0x11000,0x0,0x0,0x0,0x0,0x0,0x11d80,0x11d80,0x0,0x20000,0x11d80,0x0,0x1,0x0,0x0,0x0,0x0,0x18,0x18,0x60,0x60,0x11d80,0x2000,0x2000,0x2000,0x11d80,0x180,0x1800,0x1800,};
   }
  final private JJCalls[] jj_2_rtns = new JJCalls[3];
  private boolean jj_rescan = false;
  private int jj_gc = 0;

  /** Constructor with InputStream. */
  public JAMParser(java.io.InputStream stream) {
     this(stream, null);
  }
  /** Constructor with InputStream and supplied encoding */
  public JAMParser(java.io.InputStream stream, String encoding) {
    try { jj_input_stream = new SimpleCharStream(stream, encoding, 1, 1); } catch(java.io.UnsupportedEncodingException e) { throw new RuntimeException(e); }
    token_source = new JAMParserTokenManager(jj_input_stream);
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 71; i++) jj_la1[i] = -1;
    for (int i = 0; i < jj_2_rtns.length; i++) jj_2_rtns[i] = new JJCalls();
  }

  /** Reinitialise. */
  public void ReInit(java.io.InputStream stream) {
     ReInit(stream, null);
  }
  /** Reinitialise. */
  public void ReInit(java.io.InputStream stream, String encoding) {
    try { jj_input_stream.ReInit(stream, encoding, 1, 1); } catch(java.io.UnsupportedEncodingException e) { throw new RuntimeException(e); }
    token_source.ReInit(jj_input_stream);
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 71; i++) jj_la1[i] = -1;
    for (int i = 0; i < jj_2_rtns.length; i++) jj_2_rtns[i] = new JJCalls();
  }

  /** Constructor. */
  public JAMParser(java.io.Reader stream) {
    jj_input_stream = new SimpleCharStream(stream, 1, 1);
    token_source = new JAMParserTokenManager(jj_input_stream);
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 71; i++) jj_la1[i] = -1;
    for (int i = 0; i < jj_2_rtns.length; i++) jj_2_rtns[i] = new JJCalls();
  }

  /** Reinitialise. */
  public void ReInit(java.io.Reader stream) {
    jj_input_stream.ReInit(stream, 1, 1);
    token_source.ReInit(jj_input_stream);
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 71; i++) jj_la1[i] = -1;
    for (int i = 0; i < jj_2_rtns.length; i++) jj_2_rtns[i] = new JJCalls();
  }

  /** Constructor with generated Token Manager. */
  public JAMParser(JAMParserTokenManager tm) {
    token_source = tm;
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 71; i++) jj_la1[i] = -1;
    for (int i = 0; i < jj_2_rtns.length; i++) jj_2_rtns[i] = new JJCalls();
  }

  /** Reinitialise. */
  public void ReInit(JAMParserTokenManager tm) {
    token_source = tm;
    token = new Token();
    jj_ntk = -1;
    jj_gen = 0;
    for (int i = 0; i < 71; i++) jj_la1[i] = -1;
    for (int i = 0; i < jj_2_rtns.length; i++) jj_2_rtns[i] = new JJCalls();
  }

  private Token jj_consume_token(int kind) throws ParseException {
    Token oldToken;
    if ((oldToken = token).next != null) token = token.next;
    else token = token.next = token_source.getNextToken();
    jj_ntk = -1;
    if (token.kind == kind) {
      jj_gen++;
      if (++jj_gc > 100) {
        jj_gc = 0;
        for (int i = 0; i < jj_2_rtns.length; i++) {
          JJCalls c = jj_2_rtns[i];
          while (c != null) {
            if (c.gen < jj_gen) c.first = null;
            c = c.next;
          }
        }
      }
      return token;
    }
    token = oldToken;
    jj_kind = kind;
    throw generateParseException();
  }

  static private final class LookaheadSuccess extends java.lang.Error { }
  final private LookaheadSuccess jj_ls = new LookaheadSuccess();
  private boolean jj_scan_token(int kind) {
    if (jj_scanpos == jj_lastpos) {
      jj_la--;
      if (jj_scanpos.next == null) {
        jj_lastpos = jj_scanpos = jj_scanpos.next = token_source.getNextToken();
      } else {
        jj_lastpos = jj_scanpos = jj_scanpos.next;
      }
    } else {
      jj_scanpos = jj_scanpos.next;
    }
    if (jj_rescan) {
      int i = 0; Token tok = token;
      while (tok != null && tok != jj_scanpos) { i++; tok = tok.next; }
      if (tok != null) jj_add_error_token(kind, i);
    }
    if (jj_scanpos.kind != kind) return true;
    if (jj_la == 0 && jj_scanpos == jj_lastpos) throw jj_ls;
    return false;
  }


/** Get the next Token. */
  final public Token getNextToken() {
    if (token.next != null) token = token.next;
    else token = token.next = token_source.getNextToken();
    jj_ntk = -1;
    jj_gen++;
    return token;
  }

/** Get the specific Token. */
  final public Token getToken(int index) {
    Token t = token;
    for (int i = 0; i < index; i++) {
      if (t.next != null) t = t.next;
      else t = t.next = token_source.getNextToken();
    }
    return t;
  }

  private int jj_ntk() {
    if ((jj_nt=token.next) == null)
      return (jj_ntk = (token.next=token_source.getNextToken()).kind);
    else
      return (jj_ntk = jj_nt.kind);
  }

  private java.util.List<int[]> jj_expentries = new java.util.ArrayList<int[]>();
  private int[] jj_expentry;
  private int jj_kind = -1;
  private int[] jj_lasttokens = new int[100];
  private int jj_endpos;

  private void jj_add_error_token(int kind, int pos) {
    if (pos >= 100) return;
    if (pos == jj_endpos + 1) {
      jj_lasttokens[jj_endpos++] = kind;
    } else if (jj_endpos != 0) {
      jj_expentry = new int[jj_endpos];
      for (int i = 0; i < jj_endpos; i++) {
        jj_expentry[i] = jj_lasttokens[i];
      }
      jj_entries_loop: for (java.util.Iterator<?> it = jj_expentries.iterator(); it.hasNext();) {
        int[] oldentry = (int[])(it.next());
        if (oldentry.length == jj_expentry.length) {
          for (int i = 0; i < jj_expentry.length; i++) {
            if (oldentry[i] != jj_expentry[i]) {
              continue jj_entries_loop;
            }
          }
          jj_expentries.add(jj_expentry);
          break jj_entries_loop;
        }
      }
      if (pos != 0) jj_lasttokens[(jj_endpos = pos) - 1] = kind;
    }
  }

  /** Generate ParseException. */
  public ParseException generateParseException() {
    jj_expentries.clear();
    boolean[] la1tokens = new boolean[114];
    if (jj_kind >= 0) {
      la1tokens[jj_kind] = true;
      jj_kind = -1;
    }
    for (int i = 0; i < 71; i++) {
      if (jj_la1[i] == jj_gen) {
        for (int j = 0; j < 32; j++) {
          if ((jj_la1_0[i] & (1<<j)) != 0) {
            la1tokens[j] = true;
          }
          if ((jj_la1_1[i] & (1<<j)) != 0) {
            la1tokens[32+j] = true;
          }
          if ((jj_la1_2[i] & (1<<j)) != 0) {
            la1tokens[64+j] = true;
          }
          if ((jj_la1_3[i] & (1<<j)) != 0) {
            la1tokens[96+j] = true;
          }
        }
      }
    }
    for (int i = 0; i < 114; i++) {
      if (la1tokens[i]) {
        jj_expentry = new int[1];
        jj_expentry[0] = i;
        jj_expentries.add(jj_expentry);
      }
    }
    jj_endpos = 0;
    jj_rescan_token();
    jj_add_error_token(0, 0);
    int[][] exptokseq = new int[jj_expentries.size()][];
    for (int i = 0; i < jj_expentries.size(); i++) {
      exptokseq[i] = jj_expentries.get(i);
    }
    return new ParseException(token, exptokseq, tokenImage);
  }

  /** Enable tracing. */
  final public void enable_tracing() {
  }

  /** Disable tracing. */
  final public void disable_tracing() {
  }

  private void jj_rescan_token() {
    jj_rescan = true;
    for (int i = 0; i < 3; i++) {
    try {
      JJCalls p = jj_2_rtns[i];
      do {
        if (p.gen > jj_gen) {
          jj_la = p.arg; jj_lastpos = jj_scanpos = p.first;
          switch (i) {
            case 0: jj_3_1(); break;
            case 1: jj_3_2(); break;
            case 2: jj_3_3(); break;
          }
        }
        p = p.next;
      } while (p != null);
      } catch(LookaheadSuccess ls) { }
    }
    jj_rescan = false;
  }

  private void jj_save(int index, int xla) {
    JJCalls p = jj_2_rtns[index];
    while (p.gen > jj_gen) {
      if (p.next == null) { p = p.next = new JJCalls(); break; }
      p = p.next;
    }
    p.gen = jj_gen + xla - jj_la; p.first = token; p.arg = xla;
  }

  static final class JJCalls {
    int gen;
    Token first;
    int arg;
    JJCalls next;
  }

}
